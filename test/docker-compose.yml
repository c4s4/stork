version: "3.7"

services:

  mariadb:
    container_name: mariadb
    image: mariadb:10.4
    restart: on-failure
    env_file: .env
    ports:
    - 3306:3306
    volumes:
    - mariadb:/var/lib/mysql
    - ./test:/test
    networks:
    - default

volumes:
  mariadb:
