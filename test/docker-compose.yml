version: "3.7"

services:

  mysql:
    container_name: mysql
    image: mysql:8.0.21
    restart: on-failure
    env_file: ../.env
    ports:
    - 3306:3306
    volumes:
    - mysql:/var/lib/mysql
    - .:/sql
    networks:
    - default

volumes:
  mysql:
